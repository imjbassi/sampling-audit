__pycache__/
*.pyc
venv/
.venv/
env/

# Raw simulation trajectories are reproducible from src/alanine.py and are
# large relative to the rest of the repo; results CSVs and figures (the
# actual scientific outputs) are kept.
data/alanine/*.dcd

# Editor/OS cruft
.DS_Store
*.swp

# one-shot patch backups
*.bak
# LaTeX build products (main.pdf is committed, the rest is not)
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/tools/_body.*

# Submission-only working files can contain private contact or reviewer details.
paper/cover_letter_jctc.tex
paper/cover_letter_jctc.pdf
paper/cover_letter_jctc_*.pdf
output/
tmp/
